= État des lieux PDF/A-2b — pdf-validate vs ISO 19005-2
:toc: left
:toclevels: 2
:icons: font
:date: 2026-06-02

[abstract]
Comparaison clause par clause entre le jeu de règles de
`pdf-validate` et l'encodage de référence d'ISO 19005-2:2011
(PDF/A-2b). Objectif : savoir précisément ce qui nous manque
vis-à-vis de la norme, et prioriser la suite.

== Méthode et honnêteté de la source

Le texte de la norme ISO 19005-2 est **payant et sous copyright** ;
il n'est pas reproduit ici. La référence utilisée est l'**encodage
ouvert et machine-lisible** des clauses par le consortium veraPDF
(profil de validation PDF/A-2b, veraPDF 1.30.1, maintenu sous l'égide
de la PDF Association). C'est la même base qui sert à *certifier* un
PDF/A. Pour chaque règle veraPDF on dispose : la clause ISO, un
numéro de test, l'objet PDF visé et une description factuelle courte.

L'énumération a été obtenue par :

[source,console]
----
verapdf -f 2b --passed --format xml conforme.pdf > vera_full.xml
----

soit **144 règles** appliquées par veraPDF pour PDF/A-2b. La
couverture ci-dessous est *clause-level* : une clause est comptée
« couverte » si `pdf-validate` possède au moins un check qui l'adresse
— pas que nous répliquions chacun des numéros de test internes.

== Tableau de couverture par section (ISO 19005-2 § 6)

[cols="1,4,2,2,2",options="header"]
|===
| Section | Thème | Règles veraPDF | Couvertes | %

| § 6.1  | Structure de fichier (trailer, /ID, chiffrement, xref)     | 27  | 25  | 93 %
| § 6.2  | Graphiques — couleur, polices, transparence, images        | 56  | 41  | 73 %
| § 6.3  | Annotations                                                | 7   | 7   | 100 %
| § 6.4  | Formulaires interactifs & signatures                       | 8   | 6   | 75 %
| § 6.5  | Actions (JavaScript, /AA, actions nommées)                 | 4   | 4   | 100 %
| § 6.6  | Métadonnées (XMP, schéma d'extension PDF/A, pdfaid)        | 33  | 31  | 94 %
| § 6.8  | Fichiers embarqués                                         | 2   | 1   | 50 %
| § 6.9  | Contenu optionnel (calques OCG)                            | 4   | 4   | 100 %
| § 6.10 | Présentations alternatives                                 | 2   | 2   | 100 %
| § 6.11 | Clé Requirements                                          | 1   | 1   | 100 %
| *Total* | *PDF/A-2b*                                                | *144* | *122* | *84 %*
|===

NOTE: Chiffres régénérés le 2026-06-03. Lot 1 (§ 6.2.5 fonctions de
transfert, § 6.2.9 XObjects PostScript/référence/OPI, § 6.2.10 blend
modes), lot 2 (§ 6.2.3 profil ICC de l'output intent, § 6.2.6
rendering intent, § 6.2.8 clés du dictionnaire image), puis § 6.3
Annotations en entier (§ 6.3.1 types autorisés, § 6.3.2 drapeaux /F,
§ 6.3.3 apparences). La section Graphiques passe de 7 à 25 règles
couvertes, Annotations de 0 à 7 (100 %), le total de 26 à 51.
+
Puis lot § 6.1/§ 6.10/§ 6.11 (2026-06-03) : § 6.1.7.1 (streams sans
référence de fichier externe), § 6.1.7.2 (filtres autorisés, pas de
LZWDecode), § 6.10 (pas d'/AlternatePresentations ni de /PresSteps),
§ 6.11 (pas de /Requirements). § 6.10 et § 6.11 passent à 100 %, le
total de 51 à 58 (40 %).
+
Enfin lot « octets bruts » (0.10.0) : le `Context` reçoit les octets
du fichier (premier accès bas-niveau du validateur), permettant
§ 6.1.2 (en-tête %PDF-1.n + marqueur binaire) et § 6.1.3 t3 (pas de
données après le dernier %%EOF). § 6.1 passe à 9/27, le total à
60/144 (41 %).
+
Puis lot « tokeniseur lexical » (0.11.0, `ByteScanner`) : § 6.1.6
(chaînes hexadécimales paires et hex), § 6.1.7.1 t2 (EOL des mots-clés
stream/endstream), § 6.1.9 (espacement à un seul blanc des objets et
références indirects). Le scanner saute commentaires, chaînes
littérales et données de stream pour ne voir que la syntaxe PDF.
§ 6.1 passe à 12/27, le total à 63/144 (43 %). Reste § 6.1.7.1 t1
(/Length déclaré == longueur réelle) — reporté : /Length souvent
indirect et l'EOL avant endstream est ambigu, risque de faux positif.
+
Enfin lot « limites d'implémentation » (0.12.0) : § 6.1.13 via le
graphe d'objets — plages entier/réel, longueurs de noms/chaînes,
nombre d'objets indirects, colorants DeviceN, tailles des boîtes de
page. Huit des dix tests couverts (t8 q/Q et t10 CID exclus). § 6.1
passe à 20/27 (74 %), le total à 71/144 (49 %).
+
Lot « XMP renforcé » (0.13.0) : § 6.6.2.1 (en-tête du paquet XMP sans
attribut `bytes`/`encoding`, XML bien formé) et § 6.6.4 (conformance
∈ A/B/U) sont désormais *réellement* vérifiés, là où le validateur ne
faisait qu'attester la présence. **Le pourcentage ne bouge pas** : ces
deux clauses étaient déjà comptées au niveau clause ; ce lot réduit
l'écart « optimiste clause-level vs réel » plutôt que d'ajouter des
clauses. C'est un progrès de substance, pas de métrique.
+
Puis **interpréteur de content stream — jalon 1** (0.14.0,
`ContentStreamScanner`) : § 6.2.2 t1 (tout opérateur doit être défini
dans ISO 32000-1) et § 6.1.13 t8 (pile graphique q/Q imbriquée au plus
28 niveaux), désormais couvert — il sort de `not_covered_tests`. § 6.1
passe à 21/27, § 6.2 à 26/56, le total à 73/144 (50 %).
+
Enfin **interpréteur de content stream — jalon 2** (0.15.0) :
§ 6.2.4.3 couleur posée dans le contenu via `rg`/`RG`/`k`/`K`/`g`/`G`.
DeviceRGB exige un OutputIntent RGB (ou DefaultRGB), DeviceCMYK un
OutputIntent CMYK (ou DefaultCMYK), DeviceGray n'importe quel
OutputIntent (ou DefaultGray) — le critère exact de veraPDF
(`gOutputCS`). **Le pourcentage ne bouge pas** : § 6.2.4.3 était déjà
comptée (via la règle « images »), mais la couverture passe des seules
images XObject aux opérateurs de couleur des content streams (t2/t3/t4
réellement vérifiés). Substance, pas métrique.
+
Puis **interpréteur de content stream — jalon 3** (0.16.0) : § 6.2.6
rendering intent via l'opérateur `ri` (le scanner capture le nom-
opérande et le valide), complétant la couverture de la clause (qui ne
regardait que le `/Intent` des images). Là encore le pourcentage ne
bouge pas (§ 6.2.6 déjà comptée). Le glyphe `.notdef` (§ 6.2.11.8) est
**reporté** : il exige de résoudre les glyphes (décoder les codes →
index de glyphe via l'encodage/CMap de chaque police), bien au-delà
d'un tokeniseur — c'est le modèle de police complet de veraPDF.
+
Enfin lot § 6.8/§ 6.9 (0.17.0) : § 6.8 t2 (une spécification de
fichier avec /EF doit porter /F et /UF) et § 6.9 (configurations OCG :
/Name non vide t1, unique t2, pas d'/AS t4). § 6.8 passe à 1/2, § 6.9
à 3/4, le total à 77/144 (53 %). Reportés : § 6.8 t5 (le fichier
embarqué doit lui-même être PDF/A — validation récursive) et § 6.9 t3
(/Order doit lister tous les OCG — parcours de l'arbre Order), tous
deux exclus du décompte via `not_covered_tests`.
+
Enfin lot § 6.4/§ 6.2.4.2 (0.18.0) : § 6.4.1 (Widgets/champs sans /A ni
/AA, /NeedAppearances absent ou false), § 6.4.2 (pas de /XFA ni de
/NeedsRendering) et § 6.2.4.2 t1 (profil ICC d'un espace ICCBased :
classe et espace couleur valides). § 6.4 passe de 0 à 5/8, § 6.2 à
27/56, le total à 83/144 (57 %). Reportés : § 6.4.3 t1-t3 (ByteRange et
PKCS#7 de la signature — parsing cryptographique) et § 6.2.4.2 t2
(overprint mode ICCBased CMYK — état graphique).
+
Enfin **parseur XMP/RDF — schéma d'extension** (0.19.0,
`XmpExtensionSchema`) : § 6.6.2.3 — structure RDF des schémas
d'extension PDF/A (conteneur rdf:Bag, champs requis de pdfaSchema /
pdfaProperty / pdfaType / pdfaField, catégorie internal/external, pas
de champ non défini). Le parseur navigue par *URI de namespace* (et
non par préfixe), ce qui valide d'un coup la présence du champ et son
namespace. Calé sur veraPDF (schéma valide → 0 violation ; sans
prefix → t4 ; category invalide → t9). 19 des 21 règles couvertes ;
§ 6.6.2.3.1 (chaque propriété XMP doit appartenir à un schéma
prédéfini ou défini) reporté — il faut la liste complète des schémas
XMP 2005. § 6.6 passe de 12 à **31/33 (94 %)**, le total à
**102/144 (70 %)**.
+
Enfin lot § 6.4.3 (0.20.0) : § 6.4.3 t1 — le /ByteRange d'une
signature doit couvrir tout le document (début à l'octet 0, second
segment finissant en fin de fichier ; le trou est la zone /Contents).
Calcul d'octets sur le dictionnaire de signature et la taille du
fichier, sans cryptographie. § 6.4 passe à 6/8, le total à
**103/144 (71 %)**. Reportés (exclus via `not_covered_tests`) :
§ 6.4.3 t2-t3 (certificat de signature et nombre de SignerInfo dans le
PKCS#7) — ils exigent un parseur ASN.1/DER qui a naturellement sa
place dans le projet `aloli-crystal/pdf-signature`, et un corpus de
PDF réellement signés.
+
Enfin lot § 6.2.11 polices (0.21.0) : § 6.2.11.2 (sous-type de police
défini, /BaseFont présent, /FirstChar/LastChar/Widths pour une police
simple non standard, sous-type de /FontFile3) et § 6.2.11.3.2 (un
CIDFontType2 embarqué porte /CIDToGIDMap) — tout au niveau des
dictionnaires de police, sans programme de police. § 6.2 passe à
35/56, le total à **111/144 (77 %)**.
+
Enfin lot § 6.2.11.3.1 (0.22.0) : cohérence du CMap d'une police Type0
avec sa CIDFont descendante. Quand l'encodage n'est pas Identity-H/V
mais un flux CMap embarqué, son /CIDSystemInfo (Registry + Ordering)
doit correspondre à celui de la CIDFont — sinon les CID seraient
réinterprétés dans un autre jeu de caractères. Les polices Identity
(le cas courant de notre shard `pdf`) sont hors champ et passent sans
faux positif. § 6.2 passe à 36/56, le total à **112/144 (77 %)** — le
plancher reste 77 %, mais une règle de plus est réellement vérifiée.
Restent en § 6.2.11 les tests qui exigent le **modèle de glyphes**
(§ 6.2.11.4.1 t2 glyphes définis, § 6.2.11.5 largeurs, § 6.2.11.8
.notdef) ou de parser le programme de police (§ 6.2.11.6 t1/t4 table
cmap TrueType), plus § 6.2.11.4.2 (CharSet/CIDSet listent tous les
glyphes).
+
Enfin lot § 6.2.10 t2 + § 6.9 t3 (0.23.0). **§ 6.2.10 t2** (substance) :
une page contenant de la transparence (groupe /Group /S /Transparency,
ou un ExtGState de ses ressources utilisant un soft mask, un blend mode
non standard ou une alpha < 1) doit porter un /Group avec /CS si le
document ne déclare pas d'OutputIntent PDF/A. Comme veraPDF, on
court-circuite à « passé » dès qu'un OutputIntent fixe l'espace de
fusion (`gOutputCS`) — or tous nos PDF conformes en ont un, d'où 0 faux
positif. § 6.2.10 t2 était déjà comptée au niveau clause (via les blend
modes t1) : le décompte ne bouge pas, c'est la vérification réelle qui
progresse. **§ 6.9 t3** (métrique) : si une configuration de contenu
optionnel porte une clé /Order, ce tableau doit référencer **tous** les
OCG du fichier (les sous-groupes sont des tableaux imbriqués avec un
libellé optionnel, aplatis). Ce test sort de `not_covered_tests` :
§ 6.9 passe à **4/4 (100 %)**, le total à **113/144 (78 %)**.
+
Enfin lot § 6.1.12 (0.24.0) : dictionnaire de permissions du catalog.
t1 — /Perms ne contient que les clés /UR3 et /DocMDP ; t2 — si /DocMDP
est présent, aucun dictionnaire de référence de signature
(/Type /SigRef) ne porte /DigestLocation, /DigestMethod ou
/DigestValue. Pur dict-level, aucune crypto. Nos PDF et le corpus n'ont
pas de /Perms → 0 faux positif. § 6.1 passe à **23/27 (85 %)**, le
total à **115/144 (79 %)**.
+
Enfin lot § 6.2.11.3.3 (0.25.0) : restrictions sur les CMaps des polices
Type0. t1 — l'/Encoding est soit une CMap prédéfinie (les 61 noms de la
Table 118 d'ISO 32000-1), soit un flux CMap embarqué ; tout autre nom
est interdit. t2 — pour une CMap embarquée, le /WMode du dictionnaire
doit égaler le WMode déclaré dans le contenu du flux (`/WMode n def`).
t3 — une CMap ne référence via /UseCMap qu'une CMap prédéfinie. Nos
polices (Identity-H) passent t1 ; le corpus (Identity-H ou CMaps
embarquées valides) ne déclenche aucun faux positif. § 6.2 passe à
**39/56 (70 %)**, le total à **118/144 (81 %)**.
+
Enfin lot § 6.2.4.4 (0.26.0) : DeviceN/NChannel et Separation. t1 — tout
colorant *spot* d'un espace DeviceN/NChannel (un nom qui n'est ni un
colorant de process standard — Cyan/Magenta/Yellow/Black — ni listé
dans les /Process /Components des attributs) doit avoir une entrée dans
le dictionnaire /Colorants des attributs. t2 — tous les espaces
Separation portant le même nom de colorant doivent partager le même
espace alterné et la même fonction de teinte. Le corpus n'utilise ni
DeviceN ni Separation → 0 faux positif. § 6.2 passe à **41/56 (73 %)**,
le total à **120/144 (83 %)**.
+
Enfin lot § 6.1.10 (0.27.0) : filtre des images en ligne. Le
`ContentStreamScanner` suit l'état BI…ID d'une image en ligne et
examine la valeur de la clé /F (ou /Filter), nom unique ou tableau ; un
filtre /LZW (abrégé) / /LZWDecode ou /Crypt est interdit. Le corpus
n'utilise pas d'images en ligne LZW → 0 faux positif. § 6.1 passe à
**24/27 (89 %)**, le total à **121/144 (84 %)**.
+
Enfin lot § 6.1.4 t2 (0.28.0) : un EOL unique entre le mot-clé `xref`
et l'en-tête de sous-section. Vérifié sur les octets bruts (`@raw`),
ne concerne que les tables xref classiques (les fichiers à flux XRef
n'ont pas de mot-clé `xref`) ; `startxref` est exclu (son `xref` n'est
pas en début de ligne). Les 36 PDF conformes du corpus utilisent une
table classique à EOL unique → 0 faux positif. § 6.1 passe à
**25/27 (93 %)**, le total à **122/144 (84 %)**.
+
NOTE: § 6.1.8 t1 (« les noms de polices, de colorants et de types de
structure, après expansion des séquences `#XX`, doivent être de l'UTF-8
valide ») est **différé**, et non compté : le parseur du shard `pdf`
décode chaque `#XX` en *codepoint* Unicode puis le réencode en UTF-8
(`parse_name`), si bien qu'un nom est *toujours* UTF-8 valide au niveau
du modèle objet — la séquence d'octets d'origine est perdue. Détecter
un nom non-UTF-8 exigerait soit que le reader préserve les octets
bruts, soit une corrélation byte-level entre positions de noms et rôles
sémantiques (police/colorant/type de structure). Reporté honnêtement.
Couverture clause-level partielle assumée pour quelques clauses : les
contrôles « octets bruts » ne sont pas (encore) implémentés faute de
scanner d'octets, si bien que § 6.1.3 (on couvre t1 /ID + t2 /Encrypt,
pas t3 « pas de données après %%EOF ») et § 6.1.7.1 (on couvre t3
fichier externe, pas t1 longueur /Length ni t2 EOL stream) sont
comptées au niveau clause. De même § 6.2.5 (TR/TR2/HTP, pas les
détails halftone) et § 6.6.4 (pdfaid présent, pas la validité fine du
schéma). C'est la limite connue du décompte clause-level.
Honnêteté du comptage : § 6.2.8.3 (codestream JPEG2000) est un
descendant de § 6.2.8 mais un sous-domaine distinct *non* implémenté ;
il est explicitement exclu du décompte dans `run.sh` pour ne pas
gonfler la couverture. De même, § 6.2.5 et § 6.6.4 sont comptées au
niveau clause alors que seuls leurs tests « dictionnaire » sont
vérifiés (pas les détails halftone, ni la validité fine du schéma
XMP) — couverture clause-level partielle, comme annoncé.

== Lecture honnête du chiffre

* **78 % clause-level** est la couverture réelle de `pdf-validate`
  vis-à-vis de la norme telle que veraPDF l'encode. Trois couches
  d'analyse ont été construites successivement : le *graphe d'objets*,
  un *scanner d'octets bruts* (0.10.0), un *tokeniseur lexical*
  `ByteScanner` (0.11.0) et enfin un *interpréteur de content stream*
  `ContentStreamScanner` (0.14.0) qui tokenise les opérateurs des flux
  de contenu (en sautant opérandes, chaînes et images en ligne).
* `§ 6.1.13` est comptée au **niveau test** (granularité fine) plutôt
  qu'au niveau clause, car ses dix tests sont des limites sans rapport
  entre elles : on en couvre huit (entiers, réels, longueurs de
  noms/chaînes, nombre d'objets, colorants DeviceN, tailles de boîtes
  de page) et on exclut explicitement t8 (imbrication q/Q) et t10
  (valeur CID max) du décompte — voir `not_covered_tests` dans
  `run.sh`.
* Cela ne contredit PAS le « 60/60 » et le « 4/4 » mesurés
  précédemment : ces scores disent que sur les documents *générés
  par la suite ALOLI* et un échantillon tiers, `pdf-validate` donne
  le **même verdict** que veraPDF. C'est vrai parce que les défauts
  que nos documents peuvent avoir tombent dans les 78 % couverts.
* Sur un PDF tiers exerçant une clause non couverte (transparence non
  isolée, annotation non conforme, signature, calque…), `pdf-validate`
  pourrait dire « conforme » là où veraPDF dirait « non conforme » :
  un **faux négatif de couverture**. C'est la limite à combler.

== Ce qui manque, par priorité

==== Déjà couvert (rappel)

* **§ 6.1.2 / § 6.1.3** — en-tête %PDF-1.n + marqueur binaire
  (≥ 4 octets > 127) ; trailer avec /ID ; pas de /Encrypt ; pas de
  données après le dernier %%EOF.
* **§ 6.1.6** — chaînes hexadécimales : nombre pair de chiffres,
  uniquement des chiffres hex.
* **§ 6.1.7.1 / § 6.1.7.2** — un stream ne référence pas de fichier
  externe (/F, /FFilter, /FDecodeParms) ; mot-clé stream suivi de
  CRLF/LF et endstream précédé d'un EOL (t2) ; filtres standard
  ISO 32000-1 (LZWDecode interdit). Reste t1 (/Length exact, reporté).
* **§ 6.1.9** — objets et références indirects séparés par un seul
  caractère blanc.
* **§ 6.1.13** — limites d'implémentation : plages entier/réel,
  longueur des noms (≤ 127) et chaînes (≤ 32767), nombre d'objets
  indirects (≤ 8388607), colorants DeviceN (≤ 32), tailles des boîtes
  de page (3 à 14400). Hors champ : t8 (q/Q) et t10 (CID).
* **§ 6.8** — une spécification de fichier avec embarqué (/EF) doit
  porter /F et /UF (t2 ; la validité PDF/A du fichier embarqué, t5,
  reste à faire).
* **§ 6.9** — configurations de contenu optionnel (OCG) : /Name non
  vide et unique, pas d'/AS (t1/t2/t4 ; la complétude de /Order, t3,
  reste à faire).
* **§ 6.10 / § 6.11 (100 %)** — pas d'/AlternatePresentations (Names)
  ni de /PresSteps (pages) ; pas de /Requirements dans le catalog.
* **§ 6.2.2 (t1)** — tout opérateur de content stream doit être défini
  dans ISO 32000-1 (interpréteur `ContentStreamScanner`).
* **§ 6.2.3** — le profil ICC de l'output intent (DestOutputProfile)
  doit être de classe « prtr »/« mntr », en espace RGB/CMYK/GRAY,
  version ICC < 5 ; profil indirect partagé si plusieurs output
  intents ; pas de /DestOutputProfileRef pour un output intent PDF/X.
* **§ 6.2.4.3** — couleur device : les images et les opérateurs de
  contenu (`rg`/`k`/`g`) en DeviceRGB/CMYK/Gray exigent l'OutputIntent
  approprié (ou un Default* dans les ressources).
* **§ 6.2.5** — un ExtGState ne doit pas porter /TR ou /HTP, et /TR2
  uniquement avec la valeur /Default (fonctions de transfert).
* **§ 6.2.6** — rendering intent parmi les quatre valeurs ISO 32000-1,
  dans les deux formes : `/Intent` des images *et* l'opérateur `ri`
  des content streams.
* **§ 6.2.8** — clés du dictionnaire image : pas d'/Alternates ni
  d'/OPI, /Interpolate à false, /BitsPerComponent valide (1/2/4/8/16,
  ou 1 pour un masque).
* **§ 6.2.9** — pas de XObject PostScript (`/Subtype /PS`,
  `/Subtype2 /PS`), de XObject de référence (`/Ref`) ni d'`/OPI`.
* **§ 6.2.10 (test 1)** — les blend modes (`/BM`) d'un ExtGState
  doivent être des modes standard ISO 32000-1.
* **§ 6.3 Annotations (100 %)** — § 6.3.1 sous-types autorisés (pas
  3D/Sound/Screen/Movie), § 6.3.2 drapeaux /F (présent sauf Popup ;
  Print posé, Hidden/Invisible/NoView/ToggleNoView à zéro), § 6.3.3
  apparences (/AP requise sauf Popup/Link/Rect dégénéré ; /AP ne
  contient que /N ; /N = sous-dictionnaire pour un Widget/Btn, flux
  d'apparence sinon).

=== Priorité haute (fréquent, fort impact conformité)

* **§ 6.2 transparence (reste)** — § 6.2.10 t2 (page sans output
  intent → /Group /CS) est couvert via une heuristique conservatrice
  (groupe /Group /S /Transparency, ou ExtGState avec SMask / blend mode
  non standard / alpha < 1). Restent les cas fins : groupes de
  transparence des XObjects de formulaire, sémantique isolé/knockout,
  soft masks de luminosité — au-delà du dict-level.
* **§ 6.2.8.3 JPEG2000** — conformité du codestream (canaux, METH,
  bit-depth). Explicitement exclu du décompte tant que non implémenté.
* **§ 6.2.11 polices, vérifs fines** — largeurs de glyphes cohérentes
  (Widths vs hmtx), CIDSet/CIDToGIDMap, ToUnicode, encodages TrueType
  (MacRoman/WinAnsi). On vérifie l'embarquement, pas ces détails.
* **§ 6.6 XMP (reste)** — l'en-tête du paquet (pas de
  `bytes`/`encoding`), le XML bien formé, la conformance A/B/U et la
  **structure du schéma d'extension PDF/A** (§ 6.6.2.3, parseur RDF)
  sont couverts. Reste : § 6.6.2.3.1 (chaque propriété XMP doit
  appartenir à un schéma prédéfini ou défini — liste XMP 2005),
  cohérence XMP ↔ /Info, UTF-8 explicite (§ 6.6.2.1 t5), préfixes
  pdfaid (§ 6.6.4 t4-t7).

=== Priorité moyenne

* **§ 6.1 structure (reste)** — § 6.1.7.1 t1 (/Length déclaré ==
  longueur réelle des données du stream ; reporté pour cause de
  /Length indirect et d'EOL ambigu), § 6.1.4 (EOL du mot-clé xref),
  § 6.1.8 t1 (noms UTF-8 valides — reporté : le parseur réencode `#XX`
  en codepoints, octets bruts perdus), § 6.1.13 t10 (valeur CID max,
  interpréteur CMap).
* **§ 6.8 (reste)** — validité PDF/A du fichier embarqué (§ 6.8 t5,
  validation récursive). § 6.9 est désormais complet (4/4).

=== Priorité basse (rare en pratique ALOLI)

* **§ 6.4.3 signatures (crypto, reste)** — le /ByteRange est vérifié
  (t1) ; restent l'objet PKCS#7 (certificat de signature t2,
  SignerInfo t3) qui nécessitent un parseur ASN.1/DER (idéalement
  mutualisé avec `aloli-crystal/pdf-signature`) et un corpus signé.
* **§ 6.6 schéma d'extension PDF/A** (§ 6.6.2.3, 21 règles) — la
  structure RDF des schémas d'extension (pdfaExtension / pdfaSchema /
  pdfaProperty / pdfaType / pdfaField, avec types et préfixes) ;
  nécessite un parseur XMP/RDF dédié, chantier à part entière.

== Comment progresser (rappel de la boucle)

. Élargir le corpus tiers (ocrmypdf, PDF réels variés).
. Pour chaque divergence veraPDF ↔ pdf-validate, identifier la clause.
. L'implémenter : règle YAML seule si une primitive suffit, sinon une
  nouvelle primitive dans `Checks` + accesseur `Context` (s'appuyant
  sur `each_object`).
. Re-mesurer. La couverture clause-level monte, l'accord tiers monte.

Le « 100 % de conformité à la norme » au sens strict = répliquer les
144 règles (et idéalement les numéros de test internes) et le
démontrer sur un corpus tiers diversifié. veraPDF reste l'oracle
faisant autorité ; `pdf-validate` est la première ligne rapide,
traçable à la clause, et volontairement focalisée.

== Reproductibilité

Le tableau ci-dessus est régénérable :

[source,console]
----
doc/gap-analysis/run.sh /chemin/vers/un-pdfa2b-conforme.pdf
----

(nécessite veraPDF 1.30.1 sur le PATH et python3).
